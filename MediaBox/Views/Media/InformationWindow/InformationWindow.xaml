<UserControl x:Class="SandBeige.MediaBox.Views.Media.InformationWindow.InformationWindow"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
			 xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
			 xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
			 xmlns:local="clr-namespace:SandBeige.MediaBox.Views.Media.InformationWindow"
			 xmlns:vm="clr-namespace:SandBeige.MediaBox.ViewModels.Media"
			 mc:Ignorable="d" 
			 d:DesignHeight="450" d:DesignWidth="800"
			 d:DataContext="{d:DesignInstance vm:MediaFilePropertiesViewModel}">
	<i:Interaction.Triggers>
		<l:InteractionMessageTrigger Messenger="{Binding Messenger}">
			<l:TransitionInteractionMessageAction/>
		</l:InteractionMessageTrigger>
	</i:Interaction.Triggers>
	<StackPanel>
		<Button Content="GPS設定" Command="{Binding OpenGpsSelectorWindowCommand}"/>
		<local:Single DataContext="{Binding Items[0]}"/>
		<local:Multiple/>
	</StackPanel>
	<UserControl.Resources>
		<Style TargetType="local:Single">
			<Setter Property="Visibility" Value="Collapsed">
			</Setter>
			<Style.Triggers>
				<DataTrigger 
					Binding="{Binding DataContext.Count.Value,RelativeSource={RelativeSource AncestorType=local:InformationWindow}}"
					Value="1">
					<Setter Property="Visibility" Value="Visible"/>
				</DataTrigger>
			</Style.Triggers>
		</Style>
		<Style TargetType="local:Multiple">
			<Setter Property="Visibility" Value="Visible">
			</Setter>
			<Style.Triggers>
				<DataTrigger
					Binding="{Binding DataContext.Count.Value,RelativeSource={RelativeSource AncestorType=local:InformationWindow}}"
					Value="1">
					<Setter Property="Visibility" Value="Collapsed"/>
				</DataTrigger>
			</Style.Triggers>
		</Style>
	</UserControl.Resources>
</UserControl>
