<UserControl x:Class="SandBeige.MediaBox.Views.Media.MediaView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
			 xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
			 xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
			 xmlns:local="clr-namespace:SandBeige.MediaBox.Views.Media"
			 xmlns:vm="clr-namespace:SandBeige.MediaBox.ViewModels.Album"
			 xmlns:mediaLibraryV="clr-namespace:SandBeige.MediaBox.Views.Media.Library"
			 xmlns:mediaDetailV="clr-namespace:SandBeige.MediaBox.Views.Media.Detail"
			 xmlns:mediaMapV="clr-namespace:SandBeige.MediaBox.Views.Media.Map"
			 xmlns:mediaInfoV="clr-namespace:SandBeige.MediaBox.Views.Media.InformationWindow"
			 mc:Ignorable="d" 
			 d:DesignHeight="450" d:DesignWidth="800"
			 d:DataContext="{d:DesignInstance vm:AlbumViewModel}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<DockPanel Grid.Row="0">
			<TextBlock DockPanel.Dock="Top" Text="{Binding Title.Value}"/>
			<TextBlock DockPanel.Dock="Top" Text="{Binding Count.Value}"/>
		</DockPanel>
		<ContentControl Grid.Row="1" Content="{Binding}"/>
	</Grid>
	<UserControl.Resources>
		<DataTemplate DataType="{x:Type vm:AlbumViewModel}">
			<DockPanel>
				<Grid
					DockPanel.Dock="Top"
					Visibility="{
						Binding SelectGpsMode.Value,
						Converter={StaticResource BooleanToVisibilityConverter}
					}">
					<local:GpsSelector
						DataContext="{Binding GpsSelectorViewModel.Value}"
						Height="500"/>
				</Grid>
				<StackPanel DockPanel.Dock="Right">
					<Button Content="GPS設定" Command="{Binding SetGpsCommand}"/>
					<mediaInfoV:InformationWindow DataContext="{Binding MediaFilePropertiesViewModel.Value}"/>
				</StackPanel>
				<mediaLibraryV:MediaList DataContext="{Binding }"/>
				<mediaDetailV:MediaList DataContext="{Binding }"/>
				<mediaMapV:MediaList DataContext="{Binding }"/>
			</DockPanel>
		</DataTemplate>
		<Style TargetType="mediaLibraryV:MediaList">
			<Setter Property="Visibility" Value="Collapsed"/>
			<Style.Triggers>
				<DataTrigger Binding="{Binding DisplayMode.Value}" Value="{x:Static vm:DisplayMode.Library}">
					<Setter Property="Visibility" Value="Visible"/>
				</DataTrigger>
			</Style.Triggers>
		</Style>
		<Style TargetType="mediaDetailV:MediaList">
			<Setter Property="Visibility" Value="Collapsed"/>
			<Style.Triggers>
				<DataTrigger Binding="{Binding DisplayMode.Value}" Value="{x:Static vm:DisplayMode.Detail}">
					<Setter Property="Visibility" Value="Visible"/>
				</DataTrigger>
			</Style.Triggers>
		</Style>
		<Style TargetType="mediaMapV:MediaList">
			<Setter Property="Visibility" Value="Collapsed"/>
			<Style.Triggers>
				<DataTrigger Binding="{Binding DisplayMode.Value}" Value="{x:Static vm:DisplayMode.Map}">
					<Setter Property="Visibility" Value="Visible"/>
				</DataTrigger>
			</Style.Triggers>
		</Style>
	</UserControl.Resources>
</UserControl>
