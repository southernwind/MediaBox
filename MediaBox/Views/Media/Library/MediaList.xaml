<UserControl x:Class="SandBeige.MediaBox.Views.Media.Library.MediaList"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
			 xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
			 xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
			 xmlns:local="clr-namespace:SandBeige.MediaBox.Views.Media.Library"
			 xmlns:vm="clr-namespace:SandBeige.MediaBox.ViewModels.Album"
			 xmlns:mediaVm="clr-namespace:SandBeige.MediaBox.ViewModels.Media"
			 xmlns:controls="clr-namespace:SandBeige.MediaBox.Controls.Controls;assembly=MediaBox.Controls"
			 xmlns:behaviors="clr-namespace:SandBeige.MediaBox.Controls.Behaviors;assembly=MediaBox.Controls"
			 xmlns:enum="clr-namespace:SandBeige.MediaBox.Composition.Enum;assembly=MediaBox.Composition"
			 xmlns:mediaV="clr-namespace:SandBeige.MediaBox.Views.Media"
			 mc:Ignorable="d" 
			 d:DesignHeight="450" d:DesignWidth="800"
			 d:DataContext="{d:DesignInstance vm:AlbumViewModel}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<TextBlock Text="{Binding SelectedMediaFiles.Count}"/>
		<mediaV:TwoWayBindableSelectedItemsMediaListBox
			Grid.Row="1"
			ItemsSource="{Binding Items}"
			SelectionMode="Extended"
			ScrollViewer.IsDeferredScrollingEnabled="True"
			BindableSelectedItems="{Binding SelectedMediaFiles.Value,Mode=TwoWay}"
			ScrollViewer.HorizontalScrollBarVisibility="Disabled">
			<ItemsControl.Resources>
				<ResourceDictionary>
					<ContextMenu x:Key="MediaFileListContextMenu">
						<MenuItem Header="開く">
						</MenuItem>
						<MenuItem Header="プロパティ"></MenuItem>
					</ContextMenu>
				</ResourceDictionary>
			</ItemsControl.Resources>
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<local:Media ContextMenu="{DynamicResource MediaFileListContextMenu}">
						<local:Media.InputBindings>
							<MouseBinding
								MouseAction="LeftDoubleClick"
								Command="{Binding
									DataContext.ChangeDisplayModeCommand,
									RelativeSource={RelativeSource AncestorType={x:Type local:MediaList}}
								}"
								CommandParameter="{x:Static enum:DisplayMode.Detail}"/>
						</local:Media.InputBindings>
					</local:Media>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
			<ItemsControl.ItemsPanel>
				<ItemsPanelTemplate>
					<controls:VirtualizingWrapPanel ItemHeight="100" ItemWidth="100"/>
				</ItemsPanelTemplate>
			</ItemsControl.ItemsPanel>
		</mediaV:TwoWayBindableSelectedItemsMediaListBox>
	</Grid>
</UserControl>
